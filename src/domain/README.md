# Domain (rules engine)

This folder will contain the pure chess engine/state transitions used by the UI.

Planned (v1):
- Types: `GameState`, `Move`, `Piece`, `Square` etc.
- Pure functions: `generateLegalMoves`, `applyMove`, `getGameStatus`...

(No React, no DOM.)
